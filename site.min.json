---
---
[{% for case in site.cases %}{"caseId": "{{ case.Id }}","title": "{{ case.title }}","url": "{{ case.url }}","author": "{{ case.author }}","speciality": [{% for speciality in case.speciality %}"{{speciality}}"{% unless forloop.last %}, {% endunless %}{%endfor%}],"difficulty": "{{ case.difficulty }}","video": "{{ case.video }} ","video-thumbnail": "{{ case.video-thumbnail}}","restricted": {{ case.restricted }}} {% unless forloop.last %},{% endunless %}{% endfor %}]